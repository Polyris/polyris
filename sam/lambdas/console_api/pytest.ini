[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# NOTE: AWS environment variables are now set in conftest.py
# This ensures they're available before boto3 imports, without needing pytest-env plugin
